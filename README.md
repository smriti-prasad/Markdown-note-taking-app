# Markdown-note-taking-app
A RESTful Markdown note-taking API built with Node.js and Express that uses Multer for file uploads, persistently stores notes with collision-safe filenames, performs grammar checks via the LanguageTool API, and renders Markdown content into HTML through dedicated endpoints. 
